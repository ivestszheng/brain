---
title: <% tp.file.title %>
date: <% tp.date.now("YYYY-MM-DD") %>
tags:
  - project
  - review
status: draft
---

# 开发实战复盘

> **关联需求/Bug**：[Jira/Issue 链接]

## 🌍 项目背景 (Context)

> *写在这里，防止半年后自己忘了当初为啥这么干。*

- **业务目标**：[例如：支撑双十一大促，预期 QPS 10000，允许 0.1% 错误率]
- **技术栈概览**：[例如：Java 17 + Spring Boot 3.x + Redis Cluster + RocketMQ]
- **核心架构决策**：[例如：放弃强一致性，采用最终一致性方案以换取高并发写入能力]
- **遗留包袱**：[例如：依赖了 3 年前的老版支付 SDK，文档已失效]

## 🟢 Keep：哪些代码/设计扛住了？

> *别光看Bug，也要看哪里写得漂亮，下次直接抄。*

- **设计亮点**：[例如：这次用的策略模式，后续加新渠道只改了配置，没动核心逻辑]
- **复用建议**：[沉淀到团队 Wiki / 写成通用 SDK]
- **工具/流程**：[例如：上线前跑了 SonarQube 全量扫描，提前拦住了 3 个空指针]
- **复用建议**：[加入 CI 流水线强制卡点]

## 🔴 Stop：哪些操作必须立刻叫停？

> *这是止血，不是反思。*

- **危险动作**：[例如：为了赶进度，直接在生产库跑 `UPDATE` 没加 `WHERE` 条件 / 没看索引直接 `JOIN` 大表]
- **止损红线**：[以后所有 DDL 必须走 SQL 审核平台 / 禁止直连生产库]
- **坏习惯**：[例如：`try-catch` 吞异常只打 `e.getMessage()` / 硬编码配置在代码里]
- **止损红线**：[Code Review 发现此类代码直接打回 / 配置全部走 ConfigCenter]

## 🟡 Improve：哪里写得烂但还能跑？

> *这是还债，不是重写。*

- **性能瓶颈**：[例如：循环里查数据库，N+1 问题导致接口 2s+]
- **优化方案**：[改用批量查询 + 内存组装 / 加本地缓存]
- **预期收益**：[RT 降到 200ms 以内]
- **可维护性**：[例如：这个 Service 类 2000 行，改一行怕崩三处]
- **优化方案**：[按业务域拆分，先抽离 `OrderQueryService`，下周提 PR]
- **预期收益**：[圈复杂度从 15 降到 5]

## 🔵 Start：欠的债现在必须补

> *这是填坑，不是画饼。*

- **缺失的测试**：[例如：核心退款逻辑没有单元测试，全靠人肉点]
- **第一步行动**：[今天下班前补 3 个核心 Case，覆盖率 > 80%]
- **缺失的监控**：[例如：消息队列积压了 1 小时才发现，全靠用户投诉]
- **第一步行动**：[加 Prometheus 埋点，积压 > 1000 自动告警到钉钉]
- **缺失的文档**：[例如：这个字段含义只有离职的同事知道]
- **第一步行动**：[在代码注释 + 数据字典里补上，今晚搞定]

## 📝 待办清单 (Action Items)

| 优先级 | 动作 |
| :--- | :--- |
| **P0** | [补退款接口单测] |
| **P0** | [加 MQ 积压告警] |
| **P1** | [重构循环查库] |
| **P2** | [更新数据字典] |