# Obsidian 插件（只保留插件列表，不保留插件本体）
.obsidian/plugins/*
!.obsidian/plugins/community-plugins.json

# Obsidian 工作区状态（机器特定，频繁变动，无需跨设备同步）
.obsidian/workspace.json
.obsidian/graph.json

# 系统文件
.DS_Store
Thumbs.db

# 博客同步清单（本地临时产物，不提交；旧清单存在博客仓库）
blog-sync-manifest.json
